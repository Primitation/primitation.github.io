{
  "dependencies": {
    "three": "^0.172.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.0.11"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "gh-help": "gh-pages --help",
    "deploy": "npm run build && gh-pages -d dist"
  }
}
